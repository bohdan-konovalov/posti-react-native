import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 12,
    backgroundColor: "#fff",
    borderRadius: 8,
    elevation: 2,
    marginVertical: 6,
  },
  titleText: { fontWeight: "bold", fontSize: 16, textTransform: "uppercase" },
  bodyText: { color: "#555" },
});
