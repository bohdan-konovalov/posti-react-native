import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 16,
    backgroundColor: "#FFD2D2",
    borderRadius: 8,
    margin: 16,
    alignItems: "center",
  },
  titleText: { color: "#D8000C", fontSize: 16, fontWeight: "bold" },
  messageText: { color: "#D8000C", textAlign: "center", marginTop: 4 },
});
