import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  loadingPlaceholder: {
    backgroundColor: "#ccc",
    height: 16,
    width: "100%",
    borderRadius: 4,
    marginVertical: 4,
  },
  text: { fontSize: 16, color: "#333" },
});
